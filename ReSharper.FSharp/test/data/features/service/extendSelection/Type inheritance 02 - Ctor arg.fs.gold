type Foo() =
    inherit Bar|()|

---------------------------------------------------------
type Foo() =
    inherit |Bar()|

---------------------------------------------------------
type Foo() =
    |inherit Bar()|

---------------------------------------------------------
|type Foo() =
    inherit Bar()|

---------------------------------------------------------
|type Foo() =
    inherit Bar()
|
---------------------------------------------------------
