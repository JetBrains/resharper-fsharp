// ${COMPLETE_ITEM:Equals(obj)}
module Module

let a =
    { new obj() with
        override x.ToString() = "" 
        override x.Equals(obj) = 
            let a = 1
            let b = 2
            failwith "preserved"{caret} }
