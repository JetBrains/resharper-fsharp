// ${TEXT: }
module Module

open System

match obj() with
| :? IDisposable as{caret}

> Typing [ ] char:
====================================
Lookup list (Focused):
                              [FromLightEvaluation, LocalVariablesAndParameters, NormalSelectionPriority, Other]
  disposable                  <==

> Last text control state (lookup still opened):
====================================
// ${TEXT: }
module Module

open System

match obj() with
| :? IDisposable as {caret}
