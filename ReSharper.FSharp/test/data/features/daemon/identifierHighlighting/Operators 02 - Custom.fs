module Module

let (>>>) = () in (>>>)
let (>>=) = () in (>>=)
let (>==) = () in (>==)
let (>->) = () in (>->)
let (>>.) = () in (>>.)
let (>>.) = () in (>>.)
let (.>>) = () in (.>>)
let (>->) = () in (>->)
let (>>.~) = () in (>>.~)
