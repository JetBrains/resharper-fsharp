Provided elements:
 0: Dispose():System.Void

// ${KIND:Overrides}
// ${SELECT0:Dispose():System.Void}

open System

{ new IDisposable with
    member this.Dispose() = {selstart}failwith "todo"{selend} }
