module Module

type I =
    abstract Item: index: int * foo: bool -> string with get, set

type T() =
  interface I with
    member this.Item with get (index, foo) = failwith "todo"
    member this.Item with set (index, foo) value = {selstart}failwith "todo"{selend}
