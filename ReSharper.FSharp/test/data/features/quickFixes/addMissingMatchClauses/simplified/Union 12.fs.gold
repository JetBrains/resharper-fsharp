module Say

type U =
    | A of bool * bool
    | B of bool

match None with
| Some (A (x, true)) -> ()
| Some (B true) -> ()
| None -> {selstart}failwith "todo"{selend}
| Some value -> failwith "todo"
