module Module

let x =
    let (    | zzz | ) x =
        not X

    let f (zzz x) =
        let (zzz x) = x
        x

    match true with
    | zzz x -> ()

    let _ = (|zzz|)

    let (|Id|) f x = x

    match () with
    | Id (|zzz|) 1 -> ()
